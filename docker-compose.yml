version: "3.9"

services:
  stock-analysis:
    build: .
    container_name: ai-stock-analysis
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      TAVILY_API_KEY: ${TAVILY_API_KEY}
    volumes:
      - ./results:/app/results
    stdin_open: true
    tty: true
